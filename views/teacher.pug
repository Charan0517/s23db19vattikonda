extends layout

block content
  button.btn( class="btn-primary ml-1", onclick= `dispatch(\"/teacher/create\")`) Create
  h1= title
 
  - let result = results
  div(class="container-fluid")
    div(class="row")
      each item,index in result
       - let destination = `dispatch(\"/teacher/update?id=${item._id}\")`
       - let detailDes = `dispatch(\"/teacher/detail?id=${item._id}\")`
       - let deleteDes = `dispatch(\"/teacher/delete?id=${item._id}\")`
       div(class="col-sm-3",onClick=`buy(${index})`) Item#{item.teacher_name}(#{item.salary}) is #{item.subject}
        div(class="col-sm-2")
          button.btn( class="btn-primary ml-1", onclick= destination) Update 
        div(class="col-sm-2")
          button.btn( class="btn-success ml-1", onclick= detailDes) Details
        div(class="col-sm-2")
          button.btn( class="btn-warning ml-1", onclick= deleteDes) Delete 
  script.
   function dispatch(path){
    window.location.href=path
   }
  
//-   script.
//-     function buy(thing) {
//-       thing = thing+1;
//-       alert(`You Purchased Item ${thing}`);
//-     }